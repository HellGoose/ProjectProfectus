

<% Category.all.each do |c| %>
	
	<div class="col-xs-3 categories">
		<%#= link_to "", "/projects/" + p.id.to_s, class: "project_link" %>
		
		<div>
			<button type="button" class="catButton" id=<%= c.id %>>
			<%= c.name %>
			</button>
			
		</div>
	</div>
<% end %>