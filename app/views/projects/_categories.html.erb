
<% Category.all.each do |c| %>
	
	<div class="col-xs-3 categories">
		<%#= link_to "", "/projects/" + p.id.to_s, class: "project_link" %>
		
		<div>
			<button type="button" name=<%= c.projects.size() %> class="catButton" id=<%= c.id %>>
			<%= c.name %>

			</button>
			
		</div>
	</div>
<% end %>

<div class="col-xs-3 categories">
	<button type="button" name=<%= @projects.size() %> class="catButton" id=0>
		Show All <3
	</button>			
</div>
